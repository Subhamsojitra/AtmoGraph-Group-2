# Python bytecode / cache
__pycache__/
*.py[cod]
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# Local environment / secrets (never commit real credentials)
.env

# Editor / OS
.vscode/
.idea/
.DS_Store
backend/uvicorn_output.txt

# Trained GNN checkpoints (never committed; produced by
# backend/scripts/train_gnn.py, see README "Training and deploying a
# checkpoint")
backend/checkpoints/
