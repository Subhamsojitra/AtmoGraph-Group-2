"""API routers package.

Each submodule in this package defines an ``APIRouter`` that is aggregated by
the root application in :mod:`app.main`. Only routers relevant to the current
module are imported there.
"""