# AtmoGraph backend dependencies - Modules 1 & 2
#
# Only packages required for the current modules are listed. Future modules
# (e.g. neo4j, graph tooling) will add their dependencies when implemented.
#
# Runtime dependencies
fastapi
uvicorn
pydantic
pydantic-settings
neo4j

# NLP (Module 7 - preprocessing & NER)
#
# torch is the tensor backend required by transformers. Neither package is
# downloaded by the application or by pytest; they must be installed as shown
# in the README. The NER model weights themselves (NLP_MODEL_NAME, default
# dslim/bert-base-NER) are installed out-of-band with:
#
#   python -c "from transformers import pipeline; \
#     pipeline('token-classification', model='dslim/bert-base-NER', \
#              aggregation_strategy='simple', device='cpu')"
#
# and are cached in the HuggingFace cache directory for offline reuse.
torch
transformers

# Graph data preparation (Module 11 - GNN dataset)
#
# numpy backs the float32 node-feature matrix and int64 edge index of the
# GNN dataset (it is already pulled in transitively by torch; it is listed
# explicitly here because app.ml imports it directly).
numpy

# GNN model architecture (Module 12 - node-level delay regression)
#
# torch_geometric (PyG) provides the GCNConv message-passing layers of
# app.ml.model.GNNModel. The PyG CORE package is pure Python: GCNConv runs on
# the native torch scatter ops, so the compiled companion wheels
# (torch-scatter/torch-sparse) are NOT required for this project's model.
# It must be compatible with the installed torch build (2.x CPU) and Python;
# if pip cannot resolve it, install the wheel matching your torch version
# (see https://pytorch-geometric.readthedocs.io > Installation).
torch_geometric

# Test dependencies
httpx
pytest